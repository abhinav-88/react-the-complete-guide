import React from 'react'
const Wrapper = props => {
  return props.children; // Children prop holds all the content you're passing between the operning & closing tag off your custom component.
};
export default Wrapper;